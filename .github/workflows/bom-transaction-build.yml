name: Build workflow (bom-transaction)
run-name: Build workflow (bom-transaction)
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'bom-transaction/**'
      - '.github/workflows/bom-transaction-build.yml'
  pull_request:
    paths:
      - 'bom-transaction/**'
      - '.github/workflows/bom-transaction-build.yml'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
jobs:
  core_unit_syntax_diff:
    uses: regentmarkets/github-actions/.github/workflows/core_unit_syntax_diff.yml@master
    secrets: inherit
    with:
      repository: regentmarkets/bom-core/bom-transaction
  dependent:
    if: ${{ github.event_name == 'pull_request' }}
    uses: ./.github/workflows/bom-transaction-dependent.yml
    secrets: inherit
