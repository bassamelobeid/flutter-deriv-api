name: Daily workflow (bom-marketdataautoupdater)
run-name: Daily workflow (bom-marketdataautoupdater)
on:
  pull_request:
  workflow_dispatch:
  workflow_call:
  schedule:
    - cron: '25 11 * * *'
jobs:
  core_unit_syntax:
    uses: regentmarkets/github-actions/.github/workflows/core_unit_syntax.yml@gh_actions/combine_duplicated_workflows
    secrets: inherit
    with:
      repository: regentmarkets/bom-core/bom-marketdataautoupdater
