name: Build workflow (bom-postgres)
run-name: Build workflow (bom-postgres)
env:
  PERL_TEST_HARNESS_DUMP_TAP: /tmp/test-output
on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths: 
      - 'bom-postgres/**'
      - '.github/workflows/bom-postgres-build.yml'
  pull_request:
    paths:
      - 'bom-postgres/**'
      - '.github/workflows/bom-postgres-build.yml'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
jobs:
  core_unit_syntax_diff:
    runs-on: ubuntu-latest
    container:
      image: ${{ vars.DEBIAN_CI }}
      credentials:
        username: circle4regentmarkets
        password: ${{ secrets.DOCKER_PASSWORD }}
      options: --user nobody:nogroup
    defaults:
      run:
        shell: bash -le {0}
        working-directory: /home/git/regentmarkets/bom-core/bom-postgres
    env:
      LOG_LEVEL: debug
    steps:
      - name: setup CI step
        uses: regentmarkets/github-actions/ci-setup@master
        with:
          read_github_ssh_key: ${{ secrets.READ_GITHUB_SSH_KEY }}
          repository: regentmarkets/bom-core/bom-postgres
      - name: Code structure and syntax
        run: make syntax_diff
      - name: unit tests
        run: make unit
      - name: Core tests
        run: |
          sudo tee -a /etc/hosts < hosts.db
          cat /etc/hosts
          make test
      - name: Update UID for nobody user
        uses: regentmarkets/github-actions/update-uid@master
        if: ${{ !cancelled() }}
      - name: Upload Test Results
        uses: actions/upload-artifact@26f96dfa697d77e81fd5907df203aa23a56210a8 #v4.3.0
        if: ${{ !cancelled() }}
        with:
          name: test-results-core_unit_syntax_diff
          path: /tmp/test-output/**/*.xml
          retention-days: 1
  dependent:
    if:  ${{ github.event_name == 'pull_request' }}
    uses: ./.github/workflows/bom-postgres-dependent.yml
    secrets: inherit
