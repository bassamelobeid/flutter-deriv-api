name: Daily workflow (bom-feed-plugin)
run-name: Daily workflow (bom-feed-plugin)
on:
  schedule:
    - cron: '47 20 * * *'
jobs:
  core_syntax:
    uses: regentmarkets/github-actions/.github/workflows/core_unit_syntax.yml@master
    secrets: inherit
    with:
      repository: regentmarkets/bom-core/bom-feed-plugin
      need_unit: false
