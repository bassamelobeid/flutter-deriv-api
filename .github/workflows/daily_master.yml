name: Daily master test
run-name: Daily test on master branch
on:
  # schedule:
  #   - cron: '4 20 * * *'
  pull_request:
jobs:
  syntax:
    uses: regentmarkets/github-actions/.github/workflows/syntax.yml@dev
    secrets: inherit
  core:
    uses: regentmarkets/github-actions/.github/workflows/core.yml@dev
    secrets: inherit
