name: Code coverage workflow
run-name: Code coverage workflow
on:
  workflow_dispatch:
  push:
    branches:
      - master
  pull_request:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true
jobs:
  check_coverage_flag:
    uses: regentmarkets/github-actions/.github/workflows/check_coverage_flag.yml@master
  code_coverage:
    needs: check_coverage_flag
    if: github.ref == 'refs/heads/master' || needs.check_coverage_flag.outputs.need_coverage == 'yes'
    uses: regentmarkets/github-actions/.github/workflows/code_coverage.yml@master
    secrets: inherit
    with:
      env_vars: |
        OAUTH_CONFIG=/home/git/regentmarkets/bom-oauth/oauth.conf.example
