name: Daily workflow (bom-test)
run-name: Daily workflow (bom-test)
on:
  workflow_dispatch:
  workflow_call:
  schedule:
    - cron: '06 11 * * *'
jobs:
  syntax_unit:
    uses: regentmarkets/github-actions/.github/workflows/syntax_unit.yml@master
    secrets: inherit
    with:
      repository: regentmarkets/bom-core/bom-test
  core:
    uses: regentmarkets/github-actions/.github/workflows/core.yml@master
    secrets: inherit
    with:
      repository: regentmarkets/bom-core/bom-test
