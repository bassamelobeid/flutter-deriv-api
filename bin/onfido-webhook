#!/usr/bin/env perl
use strict;
use warnings;

use BOM::Platform::Onfido::Webhook;
use Log::Any::Adapter qw(DERIV), stderr => 'json', log_level => 'info';

return BOM::Platform::Onfido::Webhook->new if caller;

1;
