#!/etc/rmg/bin/perl

use strict;
use warnings;

# This should not be run directly. Instead, look at bin/rose_generation,
# which hides existing autogenerated modules from the @INC path: we need
# to start from a clean slate with none of the existing code when regenerating.

use BOM::Database::Script::GenerateRoseClasses;
exit BOM::Database::Script::GenerateRoseClasses::run(@ARGV);
