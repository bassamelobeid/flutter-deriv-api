---
global:
  isa: section
  contains:
    quants:
      isa: section
      contains:
        internal:
          isa: section
          contains:
            custom_client_limits:
              isa: json_string
              global: 1
              default: '{}'
