alias:
- &1
  add_ssh_keys:
    fingerprints:
    - 26:72:b1:08:0f:c1:3b:2b:2d:ea:61:7d:d6:11:63:fb
- &2
  run:
    command: /opt/circle/prepare-test.sh
    name: Update git repositories
- &3
  run:
    background: true
    command: |
      sudo mkdir -p /var/lib/binary && sudo chown nobody:nogroup /var/lib/binary # this line should be dropped when we switch json schema v3 to v4
      start_services
      touch /tmp/services.ready
    name: Start services
- &4
  run:
    command: |
      count=0
      while [ ! -f /tmp/services.ready ]; do
          echo 'Still waiting for services...'
          (( count++ )) && (( count >= 20 )) && break
          sleep 1
      done
    name: Wait for services to start up
- &5
  environment:
    PERL_TEST_HARNESS_DUMP_TAP: /var/tmp/test-output
    RPC_CONFIG: /home/git/regentmarkets/bom-rpc/rpc.conf.travis
    WEBSOCKET_CONFIG: /home/git/regentmarkets/binary-websocket-api/websocket.conf.example
default: &6
  docker:
  - auth:
      password: $DOCKERHUB_PASSWORD
      username: circle4regentmarkets
    image: regentmarkets/debian-ci:stable
    user: nobody
  shell: /bin/bash -leo pipefail
  working_directory: /home/git/regentmarkets/binary-websocket-api
jobs:
  accounts:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make accounts
        name: Accounts-related functionality
    - store_test_results:
        path: /var/tmp/test-output
  misc:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make misc
        name: Uncategorised tests
    - store_test_results:
        path: /var/tmp/test-output
  pricing:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - run:
        background: true
        command: perl /home/git/regentmarkets/bom-platform/bin/notify_pub.pl 2>&1
          | grep -Ev '(connection has been closed unexpectedly)|(terminating connection
          due to administrator)'
        name: inotify dÃ¦mon
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make pricing
        name: Pricing
    - store_test_results:
        path: /var/tmp/test-output
  schema:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - run:
        background: true
        command: perl /home/git/regentmarkets/bom-platform/bin/notify_pub.pl 2>&1
          | grep -Ev '(connection has been closed unexpectedly)|(terminating connection
          due to administrator)'
        name: inotify dÃ¦mon
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make schema
        name: Schema tests
    - store_test_results:
        path: /var/tmp/test-output
  security:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make security
        name: Security handling
    - store_test_results:
        path: /var/tmp/test-output
  structure:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make structure
        name: Code structure and syntax
    - store_test_results:
        path: /var/tmp/test-output
  subscriptions:
    <<: *6
    steps:
    - *1
    - checkout
    - *2
    - *3
    - *4
    - run:
        <<: *5
        command: |
          echo '--formatter TAP::Formatter::JUnit' >> .proverc
          make subscriptions
        name: Run subscription tests
    - store_test_results:
        path: /var/tmp/test-output
version: 2
workflows:
  build-workflow:
    jobs: &7
    - structure:
        context: perl
    - accounts:
        context: perl
    - security:
        context: perl
    - pricing:
        context: perl
    - misc:
        context: perl
    - schema:
        context: perl
    - subscriptions:
        context: perl
  daily:
    jobs: *7
    triggers:
    - schedule:
        cron: 36 20 * * *
        filters:
          branches:
            only:
            - master
  tagged:
    jobs:
    - structure:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
    - accounts:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
    - security:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
    - pricing:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
    - misc:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
    - schema:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
    - subscriptions:
        context: perl
        filters:
          branches:
            ignore: /.*/
          tags:
            only: /^V\d+_.*/
  version: 2
