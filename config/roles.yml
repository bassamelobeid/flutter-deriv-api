couchdb_client:
  consumes:
    - couchdb

couchdb_server:
  provides:
    - couchdb

couchdb_master:
  inherits:
    - couchdb_server

dealing_server:

feed_distributor:
  provides:
    - bom_feed
    - rmg_unix_server
  consumes:
    - bom_raw_feed
    - rmg_unix_server

feed_client:
  consumes:
    - bom_feed

feed_relay_source:
  provides:
    - bom_raw_feed
    - bom_feed
  inherits:
    - production_unix_server

generic_rmg_web_server:
  provides:
    - private_web_server
  inherits:
    - production_unix_server

master_live_server:
  consumes:
    - volupd_ssh

production_unix_server:
  consumes:
    - rmg_unix_server
  provides:
    - rmg_unix_server
  inherits:
    - couchdb_client

