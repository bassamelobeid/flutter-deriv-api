{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title":"Contracts for Symbol Send",
  "description":"For a given SYMBOL name, get the list of currently available contracts, and the latest barrier and duration limits for each contract.",
  "properties": {
    "contracts_for": {

      "type": "object",
      "properties": {
        "symbol": {

          "type": "string",
          "description": "It is the short symbol name",
          "sample": "R_100"

        },

        "region": {

          "type": "string",
          "description": "An optional flag to determine we would like to to get contract for japan which will include a list of predefined trading period and barriers"
        }
      },
      "required": [
        "symbol"
      ]

    }
  },
  "required": [
    "contracts_for"
  ]
}
