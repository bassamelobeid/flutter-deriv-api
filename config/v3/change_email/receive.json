{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Change Email (response)",
    "description": "The result of the change email request.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "change_email": {
            "title": "change_email",
            "description": "`1`: email changed sucssesfully, `0`: failed to change email",
            "type": "integer",
            "enum": [
                0,
                1
            ]
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "change_email"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
