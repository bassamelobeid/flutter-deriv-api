{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Trading Platform: Withdrawal (response)",
    "description": "The result of Trading withdrawal request made.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "trading_platform_withdrawal": {
            "title": "trading_platform_withdrawal",
            "description": "Information about withdrawal transaction",
            "type": "object",
            "properties": {
                "transaction_id": {
                    "description": "The reference number for the related withdrawal from the wallet account",
                    "type": "integer"
                }
            }
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "trading_platform_withdrawal"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
