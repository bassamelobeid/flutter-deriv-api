{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Trading Platform: Withdrawal (request)",
    "description": "This call allows withdrawal from Trading account to a wallet account.",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "payments"
    ],
    "additionalProperties": false,
    "required": [
        "trading_platform_withdrawal",
        "amount",
        "from_account",
        "platform",
        "to_account"
    ],
    "properties": {
        "trading_platform_withdrawal": {
            "description": "Must be `1`",
            "type": "integer",
            "enum": [
                1
            ]
        },
        "amount": {
            "description": "Amount to withdraw (in the currency of the Trading account).",
            "type": "number",
            "exclusiveMinimum": true,
            "minimum": 0
        },
        "from_account": {
            "description": "Trading account login to withdraw money from.",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "platform": {
            "description": "Name of trading platform.",
            "type": "string",
            "enum": [
                "dxtrade"
            ]
        },
        "to_account": {
            "description": "Wallet account loginid to transfer money to.",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
