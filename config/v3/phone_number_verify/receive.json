{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Phone number: Verify (response)",
    "description": "Status of the phone number verification attempt.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "phone_number_verify": {
            "title": "phone_number_verify",
            "description": "1 on success",
            "type": "integer"
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "phone_number_verify"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
