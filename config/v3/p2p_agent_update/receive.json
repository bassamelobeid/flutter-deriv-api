{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "P2P Agent Update (response)",
    "description": "Returns latest information of the agent.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "p2p_agent_update": {
            "title": "p2p_agent_update",
            "description": "P2P agent update",
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "agent_id": {
                    "description": "The agent's identification number.",
                    "type": "string"
                },
                "agent_name": {
                    "description": "The agent's displayed name.",
                    "type": "string"
                },
                "client_loginid": {
                    "description": "The loginid of the agent.",
                    "type": "string",
                    "examples": [
                        "CR12345"
                    ]
                },
                "created_time": {
                    "description": "The epoch time that the client became an agent.",
                    "type": "integer"
                },
                "is_active": {
                    "description": "The activation status of the agent.",
                    "type": "integer",
                    "enum": [
                        0,
                        1
                    ]
                },
                "is_approved": {
                    "description": "The approval status of the agent.",
                    "type": "integer",
                    "enum": [
                        0,
                        1
                    ]
                }
            }
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "p2p_agent_update"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
