{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Account Authentication Request",
  "description": "This call manages Account Authentication features such as 2FA",
  "properties": {
    "account_authentication": {
      "type":"integer",
      "pattern": "^1$",
      "description": "Must be 1",
      "required": "1"
    },
    "totp_action": {
      "enum":["status" , "enable", "disable", "generate"],
      "description": "Action to be taken for managing TOTP"
    },
    "otp": {
      "type": "string",
      "description": "OTP generated by a 2FA application like Authy, etc"
    },
    "passthrough": {
      "type": "object",
      "description": "Optional field, used to pass data through the websocket, which may be retrieved via the echo_req output field."
    },
    "req_id": {
      "type": "integer",
      "description": "Optional field to map request to response"
    }
  },
  "additionalProperties": false
}
