{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Trading Platform: Deposit (request)",
    "description": "Deposit funds from wallet account to trading platform account",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "admin"
    ],
    "additionalProperties": false,
    "required": [
        "trading_platform_deposit",
        "from_account",
        "platform",
        "to_account"
    ],
    "properties": {
        "trading_platform_deposit": {
            "description": "Must be `1`",
            "type": "integer",
            "enum": [
                1
            ]
        },
        "amount": {
            "description": "Amount to deposit (in the currency of from_wallet).",
            "type": "number"
        },
        "from_account": {
            "description": "Wallet account to transfer money from",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "platform": {
            "description": "Pass the trading platform name",
            "type": "string",
            "default": "dxtrader",
            "enum": [
                "dxtrader"
            ]
        },
        "to_account": {
            "description": "Trading account login to deposit money to",
            "type": "string"
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
