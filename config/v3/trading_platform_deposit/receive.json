{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Trading Platform: Deposit (response)",
    "description": "The result of Trading platform deposit request.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "trading_platform_deposit": {
            "title": "trading_platform_deposit",
            "description": "Information about deposit transaction, or status of demo account top up.",
            "oneOf": [
                {
                    "type": "object",
                    "properties": {
                        "transaction_id": {
                            "description": "The reference number for the related deposit to the trading account",
                            "type": "integer"
                        }
                    }
                },
                {
                    "type": "integer",
                    "enum": [
                        1
                    ]
                }
            ]
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "trading_platform_deposit"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
