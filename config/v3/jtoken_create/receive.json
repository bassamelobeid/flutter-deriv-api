{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Create JToken (response)",
    "description": "JToken response.",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "jtoken_create": {
            "description": "The JToken created.",
            "type": "string"
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "jtoken_create"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
