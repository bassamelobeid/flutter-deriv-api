{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Wallet migration managment (request)",
    "description": "This API call allows managing wallet migration process.",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "admin"
    ],
    "additionalProperties": false,
    "required": [
        "wallet_migration"
    ],
    "properties": {
        "wallet_migration": {
            "title": "Wallet migration action.",
            "description": "Wallet migration action.",
            "type": "string",
            "enum": [
                "state",
                "start",
                "reset"
            ]
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
