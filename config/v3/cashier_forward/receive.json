{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Cashier Forward Response",
  "description": "",
  "properties": {
    "cashier_forward": {
      "type": "string",
      "description": "cashier url."
    },
    "echo_req": {
      "title": "Echo request",
      "description": "Echo of the request made"
    },
    "msg_type": {
      "type": "string",
      "description": "cashier_forward"
    },
    "req_id": {
      "type": "integer",
      "description": "Optional field send in request to map to response, present only when request contains req_id"
    }
  },
  "required": [
    "echo_req",
    "msg_type"
  ]
}
