{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "Copy Start Receive",
  "description": "A message with results is received",
  "properties": {
    "echo_req": {
      "title":"Echo request",
      "description":"Echo of the request made",
      "required": 1
    },
    "copy_start": {
      "type": "integer",
      "title": "Copy starting confirmation",
      "description": "Copy starting confirmation",
      "required": 1
      }
    },
    "msg_type": {
      "type": "string",
      "description":"copy_start",
      "required": 1
    },
    "req_id": {
      "type": "integer",
      "description": "Optional field send in request to map to response, present only when request contains req_id"
    }
  }
}
