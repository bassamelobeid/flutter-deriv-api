package BOM::Database::AutoGenerated::Rose::Auth::Developer;

use strict;

use base qw(BOM::Database::Rose::DB::Object::AutoBase1);

__PACKAGE__->meta->setup(
    table   => 'developers',
    schema   => 'auth',

    columns => [
        id            => { type => 'serial', not_null => 1 },
        display_name  => { type => 'varchar', length => 48, not_null => 1 },
        description   => { type => 'text' },
        contact_name  => { type => 'varchar', length => 48, not_null => 1 },
        contact_email => { type => 'varchar', length => 48, not_null => 1 },
        password      => { type => 'varchar', length => 100, not_null => 1 },
    ],

    primary_key_columns => [ 'id' ],

    unique_key => [ 'contact_email' ],

    relationships => [
        clients => {
            class      => 'BOM::Database::AutoGenerated::Rose::Auth::Client',
            column_map => { id => 'developer_id' },
            type       => 'one to many',
        },
    ],
);

1;

