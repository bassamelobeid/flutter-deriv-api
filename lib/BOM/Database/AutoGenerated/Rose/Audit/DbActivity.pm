package BOM::Database::AutoGenerated::Rose::Audit::DbActivity;

use strict;

use base qw(BOM::Database::Rose::DB::Object::AutoBase1);

__PACKAGE__->meta->setup(
    table  => 'db_activity',
    schema => 'audit',

    columns => [
        current_session_user => {type => 'text'},
        last_activity        => {
            type    => 'timestamp',
            default => '1900-01-01 00:00:00'
        },
        max_allowed_inactivity => {
            type    => 'interval',
            default => '1 day'
        },
    ],

    primary_key_columns => ['current_session_user'],
);

1;

