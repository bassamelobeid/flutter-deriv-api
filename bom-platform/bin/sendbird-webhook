#!/usr/bin/env perl

use strict;
use warnings;
use Log::Any::Adapter qw(DERIV), stderr => 'json', log_level => $ENV{SENDBIRD_LOG_LEVEL} // 'info';
use BOM::Platform::Sendbird::Webhook;

return BOM::Platform::Sendbird::Webhook->new if caller;

1;
