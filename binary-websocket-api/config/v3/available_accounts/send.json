{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Get Available Accounts (request)",
    "description": "Types of accounts available to create or link to.",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "read"
    ],
    "additionalProperties": false,
    "required": [
        "available_accounts",
        "categories"
    ],
    "properties": {
        "available_accounts": {
            "description": "Must be `1`",
            "type": "number",
            "enum": [
                1
            ]
        },
        "categories": {
            "description": "List of account categories that needs to received.",
            "type": "array",
            "items": {
                "description": "The account categories - Limited to wallets",
                "type": "string",
                "enum": [
                    "wallet"
                ]
            }
        },
        "loginid": {
            "description": "[Optional] The login id of the user. Mandatory when multiple tokens were provided during authorize.",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
