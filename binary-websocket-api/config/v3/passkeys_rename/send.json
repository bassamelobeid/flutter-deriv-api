{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Passkeys rename (request)",
    "description": "Rename the the passkey identified by the provided system id.",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "admin"
    ],
    "additionalProperties": false,
    "required": [
        "passkeys_rename",
        "id",
        "name"
    ],
    "properties": {
        "passkeys_rename": {
            "title": "Rename Passkey name",
            "description": "Must be `1`",
            "type": "integer",
            "enum": [
                1
            ]
        },
        "id": {
            "description": "The id of the passkey to rename.",
            "type": "integer"
        },
        "name": {
            "description": "The new name of the passkey.",
            "type": "string"
        },
        "loginid": {
            "description": "[Optional] The login id of the user. Mandatory when multiple tokens were provided during authorize.",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
