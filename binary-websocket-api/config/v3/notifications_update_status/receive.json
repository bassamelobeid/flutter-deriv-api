{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Notifications Update Status (response)",
    "description": "The response of the notification status update",
    "type": "object",
    "required": [
        "echo_req",
        "msg_type"
    ],
    "properties": {
        "notifications_update_status": {
            "title": "Notification Update Status",
            "description": "Returns the ids of the notifications that were updated.",
            "type": "array",
            "items": {
                "type": "integer"
            }
        },
        "echo_req": {
            "description": "Echo of the request made.",
            "type": "object"
        },
        "msg_type": {
            "description": "Action name of the request made.",
            "type": "string",
            "enum": [
                "notifications_update_status"
            ]
        },
        "req_id": {
            "description": "Optional field sent in request to map to response, present only when request contains `req_id`.",
            "type": "integer"
        }
    }
}
