{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Phone number: Challenge (request)",
    "description": "To generate and send an OTP to the client's phone number.",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "admin"
    ],
    "additionalProperties": false,
    "required": [
        "phone_number_challenge",
        "carrier",
        "email_code"
    ],
    "properties": {
        "phone_number_challenge": {
            "description": "Must be `1`",
            "type": "number",
            "enum": [
                1
            ]
        },
        "carrier": {
            "description": "The carrier sending the OTP.",
            "type": "string",
            "enum": [
                "sms",
                "whatsapp"
            ]
        },
        "email_code": {
            "description": "The code sent via email to begin the verification.",
            "type": "string"
        },
        "loginid": {
            "description": "[Optional] The login id of the user. Mandatory when multiple tokens were provided during authorize.",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
