{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Phone number: Verify (request)",
    "description": "To verify the OTP sent to the client's phone number.",
    "hidden": 1,
    "type": "object",
    "auth_required": 1,
    "auth_scopes": [
        "admin"
    ],
    "additionalProperties": false,
    "required": [
        "phone_number_verify",
        "otp"
    ],
    "properties": {
        "phone_number_verify": {
            "description": "Must be `1`",
            "type": "number",
            "enum": [
                1
            ]
        },
        "otp": {
            "description": "The OTP sent to the client's phone number.",
            "type": "string"
        },
        "loginid": {
            "description": "[Optional] The login id of the user. If left unspecified, it defaults to the initial authorized token's login id.",
            "type": "string",
            "pattern": "^[A-Za-z]+[0-9]+$"
        },
        "passthrough": {
            "description": "[Optional] Used to pass data through the websocket, which may be retrieved via the `echo_req` output field.",
            "type": "object"
        },
        "req_id": {
            "description": "[Optional] Used to map request to response.",
            "type": "integer"
        }
    }
}
