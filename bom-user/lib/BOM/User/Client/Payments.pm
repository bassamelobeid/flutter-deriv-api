package BOM::User::Client::Payments;

use strict;
use warnings;

# This is legacy code that should never have existed.
# The placeholder module here is to help track down any remnants of the code
# that still try to load it - eventually this should be deleted as well.

use Carp qw(cluck);

cluck "Please do not load this module.";

1;
