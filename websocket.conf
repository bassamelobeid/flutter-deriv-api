if (app->mode ne 'development') {
    app->log(Mojo::Log->new(
        level => 'info',
        path  => '/var/log/httpd/bom-websockets-api_trace.log'
    ));
    app->renderer->default_format('json');
}

# conf files just return a configuration hash
{
    hypnotoad => {
        listen   => ["http://*:5004"],
        workers  => 3,
        pid_file => '/var/run/bom-daemon/bom-websockets-api.pid',
        user     => 'nobody',
        group    => 'nogroup',
    }
};

