app->log(Mojo::Log->new(
    level => 'info',
    path  => '/var/log/httpd/bom-cryptocurrency_trace.log'
));

{
    hypnotoad => {
        listen   => ["http://127.0.0.1:5051"],
        workers  => 3, # Number of worker processes
        clients  => 1, # Maximum number of concurrent connections each worker process is allowed to handle
        pid_file => '/var/run/bom-daemon/bom-cryptocurrency.pid',
        user     => 'nobody',
        group    => 'nogroup',
        inactivity_timeout => 120, # Maximum amount of time in seconds a connection can be inactive before getting closed
        heartbeat_timeout => 15, # Maximum amount of time in seconds before a worker without a heartbeat will be stopped gracefully
    }
};


