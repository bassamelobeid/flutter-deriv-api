---
system:
  description: "Various parameters determining core application functionality"
  isa: section
  contains:
    send_email_to_clients:
      description: "Are we allowed to send email outside company domains?"
      isa: Bool
      default: 0
test:
  description: "This is only for testing"
  isa: section
  contains:
    string:
      description: "Test string parameter"
      isa: Str
      default: ''
    int:
      description: "Test int parameter"
      isa: Int
      default: 100
    array:
      description: "Test array parameter"
      isa: ArrayRef
      default: []
    hash:
      description: "Test hash parameter"
      isa: HashRef
      default: {}
    dynamic:
      description: "Test dynamic parameter"
      isa: Str
      default: 'mono'
      global: 1
    dynamic_global:
      description: "Test dynamic global parameter"
      isa: Str
      default: 'mono'
      global: 1
    subsection:
      description: "Test subsection"
      isa: section
      contains:
        num:
          description: "Test numeric parameter"
          isa: Num
          default: 3.14
        string:
          description: |
            One more string parameter for testing
            this one is thorously described,
            it is quite important to have good description
            for all parameters. I wish it was POD.
          isa: Str
          default: default value
        hash2:
          description: "Hash with default value"
          isa: HashRef
          default:
            num:
              description: "Test numeric parameter"
              isa: Num
              default: 3.14
            string:
              description: "One more string parameter for testing"
              isa: Str
              default: default value
            test:
              description: "Test parameter"
              isa: Any
              default: foo bar boo
