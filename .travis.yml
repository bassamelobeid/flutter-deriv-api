sudo: required
dist: precise
group: deprecated-2017Q1
language: perl
matrix:
    fast_finish: true
services:
    - redis-server
cache: apt

notifications:
    slack:
        rooms:
            - binary-group:6t92829RicGEft1kCkpKLPj9#test-failures
        on_success: never
        on_failure: always
        on_pull_requests: false

env:
    matrix:
        - TEST_SUITE=accounts
        - TEST_SUITE=security
        - TEST_SUITE=pricing
        - TEST_SUITE=misc
        - TEST_SUITE=structure
        - TEST_SUITE=schema

before_install: ./travis/before_install.sh

install: ./travis/install.sh

script: ./travis/script.sh

after_script: /tmp/travis-scripts/wait_for_ssh.sh
