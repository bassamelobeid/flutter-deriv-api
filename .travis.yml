language: perl
perl:
    - "5.14.2"
env:
    - PERL5LIB=/home/git/regentmarkets/bom-postgres/lib:/home/git/regentmarkets/bom-platform/lib:/home/git/regentmarkets/cpan/local/lib/perl5:/home/git/regentmarkets/cpan/local/lib/perl5/x86_64-linux-gnu-thread-multi:$HOME/build/regentmarkets/bom/t:/tmp/bom-utility/lib:/tmp/bom/lib:/tmp/bom-postgres/lib:$HOME/build/regentmarkets/bom-webapi/lib:/home/travis/perl5
addons:
    postgresql: "9.3"
services:
    - redis-server
    - couchdb
cache: apt
before_install:
    - git clone https://7ec86f4afd8212dddfbe24034bb7d769611ca416@github.com/regentmarkets/travis-scripts /tmp/travis-scripts
    - export PATH=/home/git/regentmarkets/cpan/local/bin:$PATH
    - sudo ifconfig lo:1 172.28.128.3
    - sudo /usr/sbin/service postgresql stop
    - sudo apt-get update
    - sudo apt-get install -y postgresql-plperl-9.3
    - sudo apt-get install -y postgresql-contrib-9.3
    - sudo mkdir -p /home/git/regentmarkets
    - sudo rm -f /tmp/unittest_*

    # carton/cpan
    - git clone https://7ec86f4afd8212dddfbe24034bb7d769611ca416@github.com/regentmarkets/cpan.git /tmp/cpan
    - pushd /tmp/cpan && ( git checkout $TRAVIS_BRANCH || true ) && popd
    - pushd /tmp/cpan && ( sudo sh build.sh ) && popd
    - sudo mv /tmp/cpan /home/git/regentmarkets/
    # bom-utility
    - git clone https://7ec86f4afd8212dddfbe24034bb7d769611ca416@github.com/regentmarkets/bom-utility.git /tmp/bom-utility
    - pushd /tmp/bom-utility && ( git checkout $TRAVIS_BRANCH || true ) && popd
    # bom
    - git clone https://7ec86f4afd8212dddfbe24034bb7d769611ca416@github.com/regentmarkets/bom.git /tmp/bom
    - pushd /tmp/bom && ( git checkout $TRAVIS_BRANCH || true ) && popd
    - sudo cp -r /tmp/bom /home/git/regentmarkets/
    # bom-platform
    - git clone https://7ec86f4afd8212dddfbe24034bb7d769611ca416@github.com/regentmarkets/bom-platform.git /tmp/bom-platform
    - pushd /tmp/bom-platform && ( git checkout $TRAVIS_BRANCH || true ) && popd
    - sudo mv /tmp/bom-platform /home/git/regentmarkets/
    # bom-postgres
    - git clone https://7ec86f4afd8212dddfbe24034bb7d769611ca416@github.com/regentmarkets/bom-postgres.git /tmp/bom-postgres
    - pushd /tmp/bom-postgres && ( git checkout $TRAVIS_BRANCH || true ) && popd
    - sudo mv /tmp/bom-postgres /home/git/regentmarkets/

install:
    - sudo mkdir -p /home/git
    - sudo cp -R /home/git/regentmarkets/bom/config/environments/development/ /etc/rmg
    - sudo /bin/su -c "echo -n 'development' > /etc/rmg/environment"
    - curl -L 'https://www.dropbox.com/s/ip98sdc84ud70jb/devbox_config.yml.tar.gz?dl=1' |sudo tar --gzip -x -C /

    - /tmp/travis-scripts/remove_postgresdb_config.sh
    - /tmp/travis-scripts/setup_clientdb.sh
    - /tmp/travis-scripts/setup_feeddb.sh
    - /tmp/travis-scripts/setup_authdb.sh
    - /tmp/travis-scripts/setup_usersdb.sh

    - export BOM_LOG4PERLCONFIG=/home/git/regentmarkets/bom/t/config/log4perl.conf
    - export PAYMENTAPI_LOG_FILE=/tmp/paymentapi.log

script:
    - perl Makefile.PL && make test && cover -test -report coveralls
